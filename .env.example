# Copy to .env, set a local database URL and generate a random operator token.
# Never put Codex credentials here. The API currently performs no inference.
DATABASE_URL=postgresql://relaydesk:replace-with-local-password@127.0.0.1:5432/relaydesk
LAB_OPERATOR_TOKEN=replace-with-at-least-32-random-characters
